apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-site-chart.fullname" . }}-test-connection"
  labels:
    {{- include "go-site-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "go-site-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
